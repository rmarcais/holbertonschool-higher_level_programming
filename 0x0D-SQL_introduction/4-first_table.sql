-- This script creates a table called first_table in the current database in my SQL server.
-- Creates the table.
CREATE TABLE IF NOT EXISTS first_table (
       id INTEGER,
       name VARCHAR(256)
)
